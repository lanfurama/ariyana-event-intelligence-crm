# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ariyana_crm
DB_USER=your_username
DB_PASSWORD=your_password

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS (for standalone backend - not needed when using vite-plugin-api)
CORS_ORIGIN=http://localhost:3000

# Gemini API Key (BACKEND ONLY - never expose to frontend)
# This is used by the backend API routes (api/src/routes/gemini.ts)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (BACKEND ONLY - never expose to frontend)
# This is used as backup when Gemini has issues (api/src/routes/gpt.ts)
# Uses GPT-4o mini model
OPENAI_API_KEY=your_openai_api_key_here

# Note: Frontend uses /api/v1/* endpoints through vite-plugin-api
# No VITE_API_URL or VITE_GEMINI_API_KEY needed - everything goes through port 3000

